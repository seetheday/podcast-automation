# Configuration Guidelines

Use this folder for stage-specific YAML/TOML files (e.g., `transcript.yaml`). Keep secrets out of source control; reference environment variables instead. Document any new config keys inside `HANDOFF.md` so other contributors know how to reproduce your setup.
